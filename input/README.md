# Input

Place raw Markdown questions here.

The formatter accepts ordinary Markdown and can normalize supported mathematical
notation into GitHub-compatible LaTeX/MathJax.

`gate_ee_set01/GATE_EE_SET_01_RELEASE_INPUT.json` is the immutable 65-row bridge from the Question Bank review manifest to aggregate Formatter release-candidate qualification. It is evidence input, not release authorization.
